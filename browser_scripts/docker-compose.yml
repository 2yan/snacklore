version: '3.8'

services:
  browser-scripts:
    build:
      context: ..
      dockerfile: browser_scripts/Dockerfile
    volumes:
      - ../browser_scripts:/app
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    stdin_open: true
    tty: true


